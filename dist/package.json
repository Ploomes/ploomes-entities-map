{
  "name": "@ploomes/ploomes-entities-map",
  "version": "1.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "publish": "npm version patch && tsc && cp package.json dist && npm publish && git push origin"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@ploomes/ploomeststypes": "^1.0.14",
    "typescript": "^4.2.3"
  }
}
