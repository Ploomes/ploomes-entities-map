{
  "name": "@ploomes/ploomes-entities-map",
  "version": "1.0.2",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "publish": "npm version patch && git push origin && tsc && cp package.json dist && npm publish "
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@ploomes/ploomeststypes": "^1.0.14",
    "typescript": "^4.2.3"
  }
}
