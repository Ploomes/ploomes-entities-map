{
  "name": "@ploomes/ploomes-entities-map",
  "version": "1.0.12",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "update:publish": "npm version patch && git push origin && tsc && cp package.json dist && git add . && git commit -m 'new version' && git push origin && npm publish"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@ploomes/ploomeststypes": "^1.0.14",
    "typescript": "^4.2.3"
  }
}
